# LIBS

Here is the `lpp.a` file from the open source launchpad pro project. However, when using the make build-toolchain used in this project in combination with the original firmwares, you will also find `*.o` firmware blobs in here.